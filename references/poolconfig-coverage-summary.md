# PoolConfig coverage summary

Generated by `eu.solven.matmul.docs.PoolConfigSweep`.

Compares the rank that `BlockSplitSearch.findBestStrategy`
returns under each of the named PoolConfigs across a
cubic + small-non-cubic shape sweep. The interesting
rows are those where a non-`simple` config produces a
strictly better rank — that's evidence that the
orbit-expansion / rectangular / derived axis genuinely
matters for at least one shape.

**Columns**: `simple` is the baseline; subsequent
columns report (rank, Δ from simple). A negative Δ means
the config found a strictly better recipe than `simple`.

## Headline

| config | shapes-improved | average Δ on improved | best Δ |
|---|---:|---:|---:|

## Per-shape detail (only rows where some config beats `simple`)

| shape | simple |
|---|---:|

_0 shapes with at least one improvement._
